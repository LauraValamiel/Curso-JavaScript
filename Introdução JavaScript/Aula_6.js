//Calcular a área do círculo

const pi = Math.PI
const raio = 10
const area = pi * Math.pow(raio, 2)//area = pi * raio *raio
console.log('A área do círculo é: ', area.toFixed(2))

//saída de outra forma
console.log(`Area do círculo: ${pi.toFixed(2)} * ${raio} * ${raio} = ${area.toFixed(2)}`)
